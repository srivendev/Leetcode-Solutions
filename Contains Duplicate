class Solution:
    def containsDuplicate(self, nums: List[int]) -> bool:
        #set
        setVal = set()
        
        for num in nums:
            if num in setVal:
                return True
            setVal.add(num)
            
        return False
